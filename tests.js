'use strict';

var assert = require('assert');

describe('Array', function() {
	
  describe('#indexOf()', function () {
	  function tests(someParam, data = 'default'){
		  
		return data; 
	  };
	  
    it('should accept params and have defaultChecked', function () {
		let result = tests();
		
		assert.equal('notSet', result);
    });
  });
});